{
  "name": "davepack-portfolio",
  "description": "Portfolio for Dave Pack",
  "version": "1.0.0",
  "author": "Dave Pack <davepack@icloud.com>",
  "dependencies": {
    "gatsby": "^1.9.247",
    "gatsby-link": "^1.6.40",
    "gatsby-plugin-react-helmet": "^2.0.10",
    "gatsby-plugin-react-native-web": "0.1.2",
    "gatsby-plugin-react-next": "^1.0.11",
    "react-helmet": "^5.2.0"
  },
  "keywords": ["portfolio, dave pack, davepack, gatsby"],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build && gh-pages -b master -d public"
  },
  "devDependencies": {
    "babel-plugin-react-native-web": "^0.6.1",
    "gh-pages": "^1.1.0",
    "prettier": "^1.12.0"
  }
}
